# SharePoint-Folder-Redirect
# SharePoint Folder Redirect  This setup makes a migrated file share appear like a normal folder to end users, while redirecting them to a synced SharePoint Online document library.  The script checks whether the user is signed into OneDrive, syncs the SharePoint library if needed, and opens the synced folder after setup.
